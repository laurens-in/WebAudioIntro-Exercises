// write a function that takes an array of note objects and transposes them one octave up
// the function signature should look like this transposeOctave(notes)

export const transposeOctave = (notes) => notes.map((n) => n * 2);
