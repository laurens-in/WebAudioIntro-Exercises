// write a function that takes an array of note objects and returns it in reverse order
// the function signature should look like this reverse(notes)

export const reverse = (notes) => [...notes].reverse();
